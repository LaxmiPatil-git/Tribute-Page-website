touch  .gitignore--- dont add mentioned files (mentions in gitignore text documnet as   "foldername/" ex :Node-module/
git config --global user.name Laxmi
git congig --global user.email "laxmipatil888@gmail.com"
git init ---to initialise git
git add . --- [git add dot]  all files will be in staging area :in hold stage(not bothered)
git commit -m "initial commit"   ---commit message
git status   ---just to see whether working tree clean or not
$ ssh-keygen -t rsa -b 4096 -C "laxmipatil888@gmail.com" ----if we set private while creating repository we nee to give authentication by following below steps

$ cat /c/Users/IP320/.ssh/id_rsa.pub -- where it shows 'Your public key has been saved in /c/Users/IP320/.ssh/id_rsa.pub' copy same and paste after writing cat


tail <path to id_rsa.pub>---to get ssh key and copy that :next go to  your git hub account settings  select ssh and deploy  key -then click on new key give key name and then pste ssh key in below tab
git remote add origin <paste ssh url.git>
git push origin master----now ur project is in github